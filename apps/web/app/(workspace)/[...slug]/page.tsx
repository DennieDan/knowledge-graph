// Every workspace URL (/analyze, /stacks/[type]/[id], …) is rendered by the
// persistent shell in the layout; see lib/routes.ts for the route map.
export default function WorkspacePage() {
  return null;
}
